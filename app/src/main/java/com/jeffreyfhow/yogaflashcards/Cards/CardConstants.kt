package com.jeffreyfhow.yogaflashcards.Cards

/**
 * Created by jeffreyfhow on 8/19/17.
 */

/**
 * Maximum absolute horizontal distance the card can move from its root position
 */
const val MAX_X_DIST = 300f

/**
 * Maximum absolute rotation the card can rotate (while being dragged)
 */
const val MAX_ROT = 10f

/**
 * Maximum absolute rotation the card can rotate (when tweened, not dragged)
 */
const val MAX_ROT_ROOTED = 50f

/**
 * Absolute distance required to toss the current card and draw the next card
 */
const val NEXT_CARD_DIST = 200f